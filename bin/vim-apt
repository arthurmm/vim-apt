#!/usr/bin/env ruby

vim_apt_dir = File.expand_path(File.join(File.dirname(__FILE__), '..', 'lib'))
$LOAD_PATH.unshift(vim_apt_dir) unless $LOAD_PATH.include?(vim_apt_dir)
require 'vim-apt'

Vim::Apt.start!
